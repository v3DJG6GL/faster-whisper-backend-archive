# Test / development dependencies. Install on top of the base requirements:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest -q
#
# numpy / av (PyAV) — needed by test_group_trim.py — come in transitively via
# faster-whisper in the base file, so only pytest is listed here.
pytest>=9.1.0
pytest-cov>=7.1.0
# Starlette's TestClient (used by the route integration tests) prefers httpx2;
# without it, TestClient falls back to httpx with a deprecation warning. Pinned
# here (Renovate keeps it current).
httpx2>=2.4.0
