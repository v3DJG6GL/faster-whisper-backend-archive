# Optional dependencies for AUTO_CONVERT_HF_MODELS=true.
# Install with:  pip install -r requirements.txt -r requirements-convert.txt
# Footprint: ~2 GB (mostly torch). Skip this file if you don't need
# auto HF→CT2 conversion at runtime.

# CT2's TransformersConverter loads the HF checkpoint via transformers.
transformers>=5.12.1

# transformers' from_pretrained() needs torch for tensor materialisation.
# CPU-only is fine — conversion never touches CUDA.
torch>=2.12.1

# Enables `low_cpu_mem_usage=True` and sharded loading; cuts peak RAM ~half.
accelerate>=1.14.0

# huggingface_hub is already a transitive dep of faster-whisper, but listed
# explicitly so the version pin is auditable here.
huggingface_hub>=1.19.0

# filelock is also already a transitive (via huggingface_hub); listed for
# the same reason — we use it directly for cross-worker conversion locking.
filelock>=3.29.4
